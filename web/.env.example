# Leave empty to use the current origin. Vite proxies it to the native node on :4100.
VITE_API_BASE_URL=

# Set to "always" for screenshots and UI work without a running node.
# Live mode fails closed when the observer is unavailable. Use `always`
# explicitly for isolated UI demos and screenshot fixture work.
VITE_DEMO_MODE=never
